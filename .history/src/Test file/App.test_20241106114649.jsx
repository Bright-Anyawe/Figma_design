import { render,describe,it, screen } from '@testing-library/react';
import App from './App';
import Header


describe('App', () => {
it("should render the app", () => {
  render(
      <App />
  );

});
 
});