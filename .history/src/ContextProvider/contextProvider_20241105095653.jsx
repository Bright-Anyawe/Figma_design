export const HeroContext = createContext("");

const ParentContext = ({}) => {


     return(
          <>
          <HeroContext.providder>
          
          </HeroContext.providder>
     )
};

export default ParentContext
